
<h2>Your Picked this even</h2>
<%= link_to 'All Events', events_path %>
<hr>
<div class="event">
  <%= render 'single_event', event: @event %>
  <%= link_to 'Edit', edit_event_path(@event) %> |
  <%= link_to 'Delete', event_path(@event), method: :delete, data: { confirm: "Are you sure you want to delete event \"#{@event.name}\" "} %>
</div>
<hr>
